# step1 とりあえず実装

- 何を求められているのかが分からなかったので作法はgeminiに聞いた
- 自己参照構造体は知ってはいたので愚直に書く
- 入力はListNode構造体の始めのポインタが与えられて、アクセスしたポインタをリストに追加していく
- もし、次のポインタが既にリストの要素であれば、このグラフはサイクルを持つと判定できる
- c言語以外だと、ハッシュテーブルなるものがあり、探索が一瞬で終わるらしいが、今回は範囲が10^4以下であり、O(N^2)でも十分動くので今は実装しない(そもそも、ハッシュ関数をhasCycle関数内で実装するのはどうなのか問題がある)
- typoでないコーディングミスが非常に多かった。頭の中でしたいことは想像できていたが、実装するのに時間がかかった

# step1.5 他人のコード、メモを見る
- 要素0個の配列に対応しておらずNGとなるパターンがあったらしい。実装時に一切考慮しておらず、今後は気を付ける
- Cycleではない判定に今回はcurrentPointがNULLになる瞬間があるはず、という予想を用いた。もしこれをcurrentPoint-\>nextがNULLになる瞬間で判定すると要素が0個の時にエラーを吐かれる。これも考慮不足
- ほとんどの人がハッシュテーブルを用いた実装だった。今回はリストで判定したが、選択肢として両方持っておきたい

# step2 コードを整える
- currentPointがList内にあるかの判定にforループを使っているため、少しネストが深くなってる
- 今回は要素数が10000までと指定されていたのでListの要素を10000にしたが、~~これをListの要素数にできたら嬉しい。それをすると今度は可変長配列になって初期化処理のコードを実装しないといけなくなる~~入力されるNodeがどれくらいの大きさなのかは分かるわけない
- カウンターをこの関数に実装するのは直接は関係ないので自然な発想ではないなと感じた(が、どうしようもなさそう)

