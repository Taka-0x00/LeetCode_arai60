# step1 とりあえず実装
- 書く内容に変更があるのは、サイクルがあるときにどこから始まるかを返り値にせよ
- 141のコードのサイクル判定で用いた添え字iをそのまま出力すればよさそう
- 返り値は~~数字~~かNULLなのに、用意されている型がListNode構造体しかなく、何をしたらよいかわからない
- 生成AIに聞いたら、返すのは数字ではなく、ポインタだった。よく見るとListNode構造体のポインタ関数だった

